{
  "name": "agents-install-configure-playwright-addons",
  "version": "1.0.0",
  "description": "This is a **handoff bundle** from Claude Design (claude.ai/design).",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "PWDEBUG=1 npx playwright test --debug",
    "test:report": "npx playwright test && npm run allure:report",
    "allure:report": "npx allure generate ./allure-results --clean -o ./allure-report || true && npx allure open ./allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "dotenv": "^17.4.2",
    "eslint": "^10.6.0",
    "eslint-plugin-playwright": "^2.10.5",
    "playwright": "^1.61.1"
  }
}
